const SocialMedia = ({ id }) => {
  return (
    <section id={id} className="min-h-screen flex items-center justify-center border-b border-gray-800">
      <h2 className="text-h2">SocialMedia Section</h2>
    </section>
  );
};

export default SocialMedia;
